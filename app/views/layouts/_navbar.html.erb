<nav class="navbar navbar-expand-lg navbar-light bg-light navbar-gradient">
  <div class="container">
    <a class="navbar-brand" href="/" style="margin-left: -5%;">
      <div class="site-name">Site24</div>
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav mx-auto">
        <li class="nav-item">
          <form class="d-flex" role="search" style="width: 600px; margin-left: 20%" action="<%= search_articles_path %>" method="get">
            <input class="form-control me-2" type="search" placeholder="Find an article" aria-label="Search" name="query">
            <button class="btn btn-outline-success" type="submit">Search</button>
          </form>
        </li>
      </ul>
      <% if current_user.present? %>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle user-name" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" style="margin-left: 20%; font-weight:bold;">
              <%= current_user.email %>
            </a>
            <ul class="dropdown-menu" aria-labelledby="navbarDropdown" style="margin-left: 10%;">
              <li><%= link_to 'My profile', user_path(current_user.id), method: :get, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= link_to 'Write an article', new_article_path, method: :get, class: "dropdown-item" %></li>
              <li><hr class="dropdown-divider"></li>
              <li><%= button_to 'Log out', session_path, method: :delete, class: "dropdown-item text-danger" %></li>
            </ul>
          </li>
        </ul>
      <% else %>
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <%= link_to "Log In", new_session_path, class: "nav-link user-name" %>
          </li>
          <li class="nav-item">
            <%= link_to "Sign Up", new_user_path, class: "nav-link user-name" %>
          </li>
        </ul>
      <% end %>
    </div>
  </div>
</nav>
